# Research Consensus Council Frontend — Environment configuration
# Copy this file to .env before running Vite build pipeline.

# Live WebSocket Deliberation Event Endpoint (points to FastAPI host)
VITE_API_WSS_URL=ws://localhost:8080

# REST API Endpoint for paper history and user controls
VITE_API_REST_URL=http://localhost:8080
